import styled from 'styled-components';

export const ChatBoxWrapper = styled.div`
  display: flex;
  
  .listFriendWrapper {
  }
  
  .chatboxWrapper {
    width: 80%;
  }
  
  .message {
      font-family: Balsami, serif;
  }
  
  .input-group {
    font-family: Balsami, serif;
  }
  
  .sender-name {
    font-family: Balsami, serif;
  }
`;
